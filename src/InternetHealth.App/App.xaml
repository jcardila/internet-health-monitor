<Application x:Class="InternetHealth.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:c="clr-namespace:InternetHealth.App.Controls"
             ShutdownMode="OnExplicitShutdown">
  <Application.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <!-- El tema claro/oscuro (Themes/Light.xaml o Dark.xaml) lo inserta ThemeManager en la posición 0 -->
      </ResourceDictionary.MergedDictionaries>

      <FontFamily x:Key="IconFont">Segoe Fluent Icons, Segoe MDL2 Assets</FontFamily>

      <c:HealthToBrushConverter x:Key="HealthBrush" />
      <c:HealthToGlyphConverter x:Key="HealthGlyph" />
      <BooleanToVisibilityConverter x:Key="BoolToVis" />
      <c:InverseBoolToVisibilityConverter x:Key="InverseBoolToVis" />
      <c:NullOrEmptyToCollapsedConverter x:Key="EmptyToCollapsed" />

      <!-- Íconos de la cadena de conexión (trazos en una caja de 24×24) -->
      <Geometry x:Key="Icon.Device">M4,5 H20 V15 H4 Z M2,18.5 H22</Geometry>
      <Geometry x:Key="Icon.Wifi">M2.5,9 C7.8,4 16.2,4 21.5,9 M5.8,12.4 C9.3,9.3 14.7,9.3 18.2,12.4 M9,15.8 C10.8,14.4 13.2,14.4 15,15.8 M11.3,19.2 A0.8,0.8 0 1 0 12.7,19.2 A0.8,0.8 0 1 0 11.3,19.2</Geometry>
      <Geometry x:Key="Icon.Cable">M7,3 H17 V12 H14.5 V16 H9.5 V12 H7 Z M12,16 V21.5 M10,6 V8.5 M14,6 V8.5</Geometry>
      <Geometry x:Key="Icon.Router">M3,13 H21 V19.5 H3 Z M6.5,16.2 H7.5 M9.5,16.2 H10.5 M8,13 L6,5.5 M16,13 L18,5.5</Geometry>
      <Geometry x:Key="Icon.Provider">M12,8.5 L7.5,21.5 M12,8.5 L16.5,21.5 M9,17 H15 M9.4,6.1 C10.9,4.7 13.1,4.7 14.6,6.1 M6.9,3.6 C9.9,0.9 14.1,0.9 17.1,3.6</Geometry>
      <Geometry x:Key="Icon.Globe">M12,3 A9,9 0 1 0 12,21 A9,9 0 1 0 12,3 Z M3,12 H21 M12,3 C8.3,6.6 8.3,17.4 12,21 M12,3 C15.7,6.6 15.7,17.4 12,21</Geometry>

      <!-- Tipografía -->
      <Style x:Key="Text.Hero" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Foreground" Value="{DynamicResource Fg.Primary}" />
      </Style>
      <Style x:Key="Text.Section" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource Fg.Primary}" />
      </Style>
      <Style x:Key="Text.Body" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="LineHeight" Value="19" />
        <Setter Property="Foreground" Value="{DynamicResource Fg.Secondary}" />
      </Style>
      <Style x:Key="Text.Caption" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Foreground" Value="{DynamicResource Fg.Muted}" />
      </Style>
      <Style x:Key="Text.Metric" TargetType="TextBlock">
        <Setter Property="FontSize" Value="22" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource Fg.Primary}" />
      </Style>

      <Style x:Key="Card" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource Bg.Card}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Border.Card}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="16" />
      </Style>

      <!-- Botones con plantilla propia: se ven igual en Windows 10 y 11, en claro y oscuro -->
      <Style x:Key="Button.Base" TargetType="Button">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Padding" Value="14,7" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Button">
              <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" RecognizesAccessKey="True" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter TargetName="Bd" Property="Opacity" Value="0.88" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                  <Setter TargetName="Bd" Property="Opacity" Value="0.75" />
                </Trigger>
                <Trigger Property="IsKeyboardFocused" Value="True">
                  <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Fg.Primary}" />
                  <Setter TargetName="Bd" Property="BorderThickness" Value="2" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter TargetName="Bd" Property="Opacity" Value="0.45" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="Button.Primary" TargetType="Button" BasedOn="{StaticResource Button.Base}">
        <Setter Property="Background" Value="{DynamicResource Accent}" />
        <Setter Property="Foreground" Value="{DynamicResource Accent.Fg}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Accent}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontWeight" Value="SemiBold" />
      </Style>
      <Style x:Key="Button.Secondary" TargetType="Button" BasedOn="{StaticResource Button.Base}">
        <Setter Property="Background" Value="{DynamicResource Bg.Subtle}" />
        <Setter Property="Foreground" Value="{DynamicResource Fg.Primary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Border.Card}" />
        <Setter Property="BorderThickness" Value="1" />
      </Style>
      <Style x:Key="Button.Icon" TargetType="Button" BasedOn="{StaticResource Button.Base}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource Fg.Secondary}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="FontFamily" Value="{StaticResource IconFont}" />
        <Setter Property="FontSize" Value="14" />
      </Style>
      <Style x:Key="Button.Range" TargetType="RadioButton">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Margin" Value="0,0,6,0" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="RadioButton">
              <Border x:Name="Bd" Background="{DynamicResource Bg.Subtle}" BorderBrush="{DynamicResource Border.Card}"
                      BorderThickness="1" CornerRadius="6" Padding="12,5">
                <ContentPresenter x:Name="Cp" TextElement.Foreground="{DynamicResource Fg.Primary}" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsChecked" Value="True">
                  <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Accent}" />
                  <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Accent}" />
                  <Setter TargetName="Cp" Property="TextElement.Foreground" Value="{DynamicResource Accent.Fg}" />
                </Trigger>
                <Trigger Property="IsKeyboardFocused" Value="True">
                  <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Fg.Primary}" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <!-- Distintivo de estado: círculo de color + símbolo (nunca solo color) -->
      <DataTemplate x:Key="StatusBadge">
        <Grid Width="22" Height="22">
          <Ellipse Fill="{Binding Converter={StaticResource HealthBrush}}" />
          <TextBlock Text="{Binding Converter={StaticResource HealthGlyph}}" FontFamily="{StaticResource IconFont}" FontSize="11"
                     Foreground="{Binding Converter={StaticResource HealthBrush}, ConverterParameter=Ink}"
                     HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
      </DataTemplate>
    </ResourceDictionary>
  </Application.Resources>
</Application>
